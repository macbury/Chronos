.block
  %h3 Z których serwisów korzystasz?
  %ul.navigation.accounts
    - self.current_user.social_accounts.all.each do |account|
      %li{ :class => cycle("alt", "normal") }
        = link_to account.name, account, :class => account.type_name
        .clear