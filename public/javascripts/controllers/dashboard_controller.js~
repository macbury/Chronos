App.Controllers.Dashboard = Backbone.Controller.extend({
  streams: null,
  
  routes: {
    "#": "index",
  },

  index: function() {
    console.log("index");
  },

  initialize: function() {
    this.streams = new App.Collections.Stream();
    this.streams.fetch();
  }
});
